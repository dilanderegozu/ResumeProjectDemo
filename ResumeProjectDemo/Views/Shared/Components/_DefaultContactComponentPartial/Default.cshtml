<section id="contact" class="contact-section">
    <style>
        .contact-section {
            padding: 80px 0;
            background: #f8f9fb;
            font-family: sans-serif;
        }

            .contact-section .container {
                max-width: 960px;
                margin: 0 auto;
                padding: 0 1.5rem;
            }

            .contact-section .section-header {
                text-align: center;
                margin-bottom: 56px;
            }

                .contact-section .section-header h2 {
                    font-size: 32px;
                    font-weight: 600;
                    color: #1a1a2e;
                    margin: 0 0 12px;
                }

                .contact-section .section-header p {
                    font-size: 15px;
                    color: #64748b;
                    max-width: 560px;
                    margin: 0 auto;
                    line-height: 1.7;
                }

        .contact-info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 16px;
            margin-bottom: 48px;
        }

        .contact-info-card {
            background: #fff;
            border: 1px solid #e8ecf0;
            border-radius: 14px;
            padding: 28px 20px;
            text-align: center;
            transition: border-color 0.2s, transform 0.2s;
        }

            .contact-info-card:hover {
                border-color: #7c6af0;
                transform: translateY(-2px);
            }

        .contact-info-icon {
            width: 48px;
            height: 48px;
            border-radius: 12px;
            background: #f0eeff;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 16px;
        }

            .contact-info-icon svg {
                width: 22px;
                height: 22px;
                stroke: #7c6af0;
                fill: none;
                stroke-width: 1.8;
            }

        .contact-info-label {
            font-size: 11px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            color: #94a3b8;
            margin-bottom: 8px;
        }

        .contact-info-card a, .contact-info-card span {
            display: block;
            font-size: 14px;
            color: #1a1a2e;
            text-decoration: none;
            line-height: 1.8;
            transition: color 0.15s;
        }

            .contact-info-card a:hover {
                color: #7c6af0;
            }

        .contact-form-wrapper {
            background: #fff;
            border: 1px solid #e8ecf0;
            border-radius: 18px;
            padding: 40px 40px 36px;
        }

            .contact-form-wrapper h3 {
                font-size: 18px;
                font-weight: 600;
                color: #1a1a2e;
                margin: 0 0 28px;
            }

        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 16px;
        }

            .form-grid .full {
                grid-column: 1 / -1;
            }

        .contact-section .field-group {
            display: flex;
            flex-direction: column;
            gap: 6px;
        }

            .contact-section .field-group label {
                font-size: 13px;
                font-weight: 500;
                color: #475569;
            }

            .contact-section .field-group input,
            .contact-section .field-group textarea {
                padding: 11px 14px;
                font-size: 14px;
                border: 1px solid #dde1e9;
                border-radius: 10px;
                background: #f8f9fb;
                color: #1a1a2e;
                outline: none;
                transition: border-color 0.2s, background 0.2s;
                font-family: inherit;
                resize: none;
            }

                .contact-section .field-group input:focus,
                .contact-section .field-group textarea:focus {
                    border-color: #7c6af0;
                    background: #fff;
                }

                .contact-section .field-group input::placeholder,
                .contact-section .field-group textarea::placeholder {
                    color: #b0b9c8;
                }

        .contact-section .submit-row {
            margin-top: 24px;
            text-align: center;
        }

        .contact-section .btn-submit {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: #7c6af0;
            color: #fff;
            border: none;
            border-radius: 10px;
            padding: 13px 36px;
            font-size: 15px;
            font-weight: 500;
            cursor: pointer;
            transition: background 0.2s, transform 0.15s;
            font-family: inherit;
        }

            .contact-section .btn-submit:hover {
                background: #6855e0;
                transform: translateY(-1px);
            }

            .contact-section .btn-submit:active {
                transform: scale(0.98);
            }

            .contact-section .btn-submit svg {
                width: 16px;
                height: 16px;
                stroke: #fff;
                fill: none;
                stroke-width: 2;
            }

        .contact-form-result {
            margin-top: 16px;
            font-size: 14px;
            color: #64748b;
        }

        @@media (max-width: 600px) {
            .form-grid {
                grid-template-columns: 1fr;
            }

                .form-grid .full {
                    grid-column: 1;
                }

            .contact-form-wrapper {
                padding: 24px 20px;
            }
            .contact-section .section-header {
    position: relative;
}

.contact-section .section-header h2 {
    position: relative;
    z-index: 1;
}

.contact-section .section-header .title-letter {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -60%);
    font-size: 120px;
    font-weight: 800;
    color: rgba(0, 0, 0, 0.04);
    line-height: 1;
    z-index: 0;
    pointer-events: none;
    user-select: none;
}
        }

        .contact-section .section-header {
            position: relative;
        }

            .contact-section .section-header h2 {
                position: relative;
                z-index: 1;
            }

            .contact-section .section-header .title-letter {
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -60%);
                font-size: 120px;
                font-weight: 800;
                color: rgba(0, 0, 0, 0.04);
                line-height: 1;
                z-index: 0;
                pointer-events: none;
                user-select: none;
            }
    </style>

    <div class="container">

        <div class="section-header">
            <h2 class="mb-0">İletişim</h2>
            <span class="title-letter">İ</span>
            <p>
                Benimle iletişime geçmek, projeler hakkında konuşmak ya da iş birliği fırsatlarını
                değerlendirmek istersen telefon, e-posta veya aşağıdaki bağlantılar üzerinden ulaşabilirsin.
            </p>
        </div>

        <div class="contact-info-grid">
            <div class="contact-info-card">
                <div class="contact-info-icon">
                    <svg viewBox="0 0 24 24"><path d="M22 16.92v3a2 2 0 0 1-2.18 2A19.79 19.79 0 0 1 11.61 19a19.5 19.5 0 0 1-6-6A19.79 19.79 0 0 1 3.09 4.18 2 2 0 0 1 5.07 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L9.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z" /></svg>
                </div>
                <div class="contact-info-label">Telefon</div>
                <a href="tel:+905010718237">+90 501 071 82 37</a>
            </div>

            <div class="contact-info-card">
                <div class="contact-info-icon">
                    <svg viewBox="0 0 24 24"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z" /><polyline points="22,6 12,13 2,6" /></svg>
                </div>
                <div class="contact-info-label">E-posta</div>
                <a href="mailto:dilan.deregizu@gmail.com">dilan.deregizu@gmail.com</a>
                <span style="font-size:12px; color:#94a3b8; margin-top:4px;">Backend Developer</span>
            </div>

            <div class="contact-info-card">
                <div class="contact-info-icon">
                    <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="10" /><line x1="2" y1="12" x2="22" y2="12" /><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z" /></svg>
                </div>
                <div class="contact-info-label">Web & Sosyal</div>
                <a href="https://www.dilanderegozu.com" target="_blank">dilanderegozu.com</a>
                <a href="https://www.linkedin.com/in/dilan-dereg%C3%B6z%C3%BC-244172340/" target="_blank">LinkedIn Profili</a>
            </div>
        </div>

        <div class="contact-form-wrapper">
            <h3>Mesaj Gönder</h3>
            <form id="cf" name="cf" action="include/sendemail.php" method="post" autocomplete="off">
                <div class="form-grid">
                    <div class="field-group">
                        <label for="cf-name">Ad Soyad</label>
                        <input type="text" id="cf-name" name="cf-name" placeholder="Adınızı girin" class="required">
                    </div>
                    <div class="field-group">
                        <label for="cf-email">E-posta</label>
                        <input type="email" id="cf-email" name="cf-email" placeholder="ornek@email.com" class="required">
                    </div>
                    <div class="field-group full">
                        <label for="cf-subject">Konu</label>
                        <input type="text" id="cf-subject" name="cf-subject" placeholder="Mesajınızın konusu">
                    </div>
                    <div class="field-group full">
                        <label for="cf-message">Mesaj</label>
                        <textarea id="cf-message" name="cf-message" placeholder="Mesajınızı buraya yazın..." class="required" rows="6"></textarea>
                    </div>
                    <div class="d-none" style="display:none;">
                        <input type="text" id="cf-botcheck" name="cf-botcheck" value="">
                    </div>
                </div>
                <div class="submit-row">
                    <button class="btn-submit" type="submit" id="cf-submit" name="cf-submit">
                        <svg viewBox="0 0 24 24"><line x1="22" y1="2" x2="11" y2="13" /><polygon points="22 2 15 22 11 13 2 9 22 2" /></svg>
                        Mesaj Gönder
                    </button>
                </div>
            </form>
            <div class="contact-form-result"></div>
        </div>

    </div>
</section>